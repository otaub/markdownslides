# Hyperparameter Search with

![Propulate Logo](https://raw.githubusercontent.com/Helmholtz-AI-Energy/propulate/refs/heads/main/LOGO_light.svg#gh-light-mode-only)

---

## Material

- Slides are here: [https://otaub.github.io/markdownslides/propulate_demo](https://otaub.github.io/markdownslides/propulate_demo)

- Demo code is here: [TODO](TODO)

---

# Motivation

- Hyperparameter Search
    - Explore the space by evaluating many candidates
        - Grid Search?
    - As few as possible
        -  Random Search
- Capitalize on already evaluated examples guide the search and to propose future candidates
    - "Intelligent" search algorithms
    - Parallel search algorithms

---

## What is Propulate?

- A tool for parallel hyperparameter search
    - Designed for HPC environments
    - One optimization job that performs many evaluations, not one job per evaluation
    - Not elastic
    - Using MPI for scaling and communication
    - Decentralized
        - Avoiding communication bottlenecks or load imbalances
    - Lazily synchronized
        - Avoiding idle workers

---

## Population Based Optimization

![https://propulate.readthedocs.io/en/latest/_images/asynchronous_pop.png](https://propulate.readthedocs.io/en/latest/_images/asynchronous_pop.png)

---

## Sampling Algorithm Example: Genetic Crossover

![https://propulate.readthedocs.io/en/latest/_images/crossover.png](https://propulate.readthedocs.io/en/latest/_images/crossover.png)

---

## Asynchronous Communication for Parallel Search

![https://propulate.readthedocs.io/en/latest/_images/async_prop_hgf.png](https://propulate.readthedocs.io/en/latest/_images/async_prop_hgf.png)

---
# Toy Example

```python
import random
from propulate import utils, Propulator

def loss_fn(params):
    return params['x']**2 + params['y']**2

limits = {'x': (-3., 3.), 'y': (-3., 3.)

[...]

propagator = get_default_propagator(pop_size, limits, rng)
propulator = Propulator(loss_fn, propagator, rng, generations, checkpoint_path)

propulator.propulate()
```

---

# Islands

![https://propulate.readthedocs.io/en/latest/_images/async_migration_hgf.png](https://propulate.readthedocs.io/en/latest/_images/async_migration_hgf.png)

---

## Loss function for NAS

- extract parameters from params argument
- initialize neural network
- set up training
- run training
- evaluate model
- return loss to propulate (can also be a different metric, but smaller = better)

---

# Worker Parallelization

#### for expensive evaluation it may make sense to parallelize

```python
islands = Islands(
    ...
    ranks_per_worker=2
    )
```

Loss function has to accept an MPI communicator
```python
def parallel_loss_fn(params, comm):
    return comm.allreduce(list(params.values())[comm.rank] ** 2)
```

---

# DDP
- wrap the dataloader sampler, model, etc. in `distributed`
- initialize process group
- set environment variables `MASTER_ADDR` and `MASTER_PORT`
- call `torch.distributed.init_process_group`

